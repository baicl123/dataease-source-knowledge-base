# 2026-07-08 工作日志

## Task 7：数据库分析（完成）
- 解析 `core/core-backend/src/main/resources/db/` 下全部 DDL：migration 18 + desktop 18 + distributed manage 1 + installer 1 = 38 个 SQL（与 TASKS.md 一致）
- 去重后解析出 **78 张表**（V2.0 基线 40 + 增量新建 38），含 11 张 QRTZ_*、10 张 snapshot_*、22 张扩展/企业表
- 分析 6 个 Ext* Mapper XML = 107 条自定义 SQL（复制/快照/恢复/模板导入）
- 产出 docs/database/ 7 篇文档：index.md、schema-core.md、schema-visualization.md、schema-extension.md、schema-quartz.md、migrations.md、mappers.md
- 关键发现：migration vs desktop 为 MySQL/H2 部署变体；V2.10.7 引入 snapshot_* 发布镜像表；xpack_*/core_copilot_* 等表 DDL 出现在社区 migration 但逻辑在 de-xpack submodule
- 更新 TASKS.md（Task 7 → ✅）、INDEX.md（数据库章节）、MEMORY.md
- 复用/新建脚本：parse_ddl.py、extract_deltas.py、aggregate_schema.py、schema_to_md.py、build_database_docs.py（均存 scripts/）
- 乐享同步：本次未重新同步（待用户确认或下次统一同步）

## 待办
- Task 8：版本升级机制（scan_source.py 已可复用做版本差异）

## 任务系统自检（用户请求）
- 权威清单 TASKS.md：仅 Task 8 未完成；Task 1-7、9 均 ✅
- 细粒度 TaskList 发现 8 条陈旧僵尸条目（#6/#35/#42/#43/#49/#50/#52/#61），对应工作早已随文档完成并 commit，仅当时未回写状态；已全部标记为 completed
- 结论：唯一真实开放项是 **Task 8 版本升级机制**（尚无升级指南文档，但前置脚本 scan_source.py 已就绪）

## 空目录自检与结构对齐（用户请求）
- 发现 6 个空目录：docs/{adr,api,deployment,glossary,plugin}/ + versions/，均未被 git 跟踪
- 根因：README 模板 prescribe 的标准 SKB 目录 vs TASKS.md 9 任务实际执行的结构错位
- api/→frontend/api-layer.md+backend/api-permissions.md；deployment/→architecture/build-deploy.md（内容在别处）；adr/glossary/plugin 真正缺失；versions/→Task 8
- 额外发现：README 引用了不存在的 TODO.md/CHANGELOG.md；metadata/ 实际仅 3 个 JSON（README 写 6 个）
- 处置（用户选「改 README 对齐现实」）：改写 README.md 目录结构+metadata 清单；补建 TODO.md/CHANGELOG.md；rmdir 清理 6 个空目录
- 遗留：docs/database/（Task 7）+ scripts/（5 个）+ TASKS.md/INDEX.md 改动仍未 commit（待用户确认提交）

## 架构图改写（用户请求）
- 原 `docs/diagrams/architecture.mmd` 有问题：单文件塞 4 个独立 Mermaid 图（非法）、节点 ID 跨图撞车（BE/PERM）、图2结构混乱、缺文字说明
- 改写为 `docs/diagrams/architecture.md`：4 张图各自独立放进 ```` ```mermaid ```` 代码块、标签保持英文、各图配中文「用途/要点/源码证据」说明
- 同步更新 4 处引用（TASKS.md / security-model.md / overview.md / INDEX.md），删除旧 .mmd
- 注：diagram 标签仍用英文（AGENTS.md 约束）；说明文字用中文

## 二次开发文档目录重组（用户请求）
- 用户新建 `docs/customization/`，要求所有二次开发相关内容（建议 + 未来实际改造）统一存放
- `git mv docs/upgrade/permission-development-guide.md → docs/customization/`（保留 git 历史）
- 修复 guide 内 6 个预存坏链：`api-permissions.md` 等原指向同目录（不存在），改为 `../backend/*.md`
- 从 `security-model.md` §6 抽取「二次开发切入点」4 条 → 移入 guide 新增 §0.1「切入点速览」；security-model §6 改为纯指针
- `overview.md` §7 增加指向 customization guide 的链接
- `diagrams/architecture.md` 图4 引用 upgrade→customization
- INDEX.md：拆「升级与二次开发」为「二次开发（Customization）✅」+「版本升级（Upgrade）🔲」；清理 plugin/api/adr/glossary 陈旧占位段（移入「待补充·未来可选」）
- README.md：目录结构增 `customization/`+`upgrade/` 两行；版本升级段注明 customization 归口
- AGENTS.md：新增 §8「项目特定约定」——customization/ 存二次开发、upgrade/ 存版本差异、交叉引用用相对路径
- TASKS.md line 37 路径更新；删除空 upgrade/ 目录
- 判断：Task 8（版本升级机制）仍归 `upgrade/`（属 KB 版本追踪，非 DataEase 定制），已在回复中说明

## Git 提交 + 推送（用户请求）
- 3 个 commit：d4dde68(database) / 83b7e9f(diagrams) / 1085d57(customization+structure)
- .gitignore 增 .DS_Store；untrack docs/.DS_Store；补建 TODO.md/CHANGELOG.md
- 添加远程 origin: git@github.com:baicl123/dataease-source-knowledge-base.git
- push 成功（main 分支，111 objects，324 KiB）

## 乐享同步（用户请求）
- 新建「数据库（Database）」文件夹（id: 29789593a346412bb816ec3ca14887b1）→ 导入 7 篇数据库文档
- 新建「二次开发（Customization）」文件夹（id: b7d9b5d583474cb9a2a7c3c370db2d99）
- 移动权限指南从「升级与二次开发」→「二次开发」文件夹（entry_move_entry）
- 更新权限指南内容（force_write 覆盖：增 §0.1 切入点速览 + customization 目录说明）
- 更新 INDEX 内容（force_write 覆盖：增数据库/二次开发/版本升级章节）
- 重命名「升级与二次开发（Upgrade）」→「版本升级（Upgrade）」（现已空，待 Task 8）
- 乐享目录现为：架构(5) / 后端(11) / 前端(12) / 模块(1) / 数据库(7) / 二次开发(1) / 版本升级(空) / INDEX
- 遗留：security-model/overview 在乐享侧内容未更新（仅指针文字微调，非核心内容变更）
